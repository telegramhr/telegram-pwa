export default ({ app, store }) => {
  if (!store.state.user.access !== 'BR92VTWM') {
    const adsbygoogle = window.adsbygoogle || []
    adsbygoogle.push({
      google_ad_client: 'ca-pub-2317149376955370',
    })
  }
}
